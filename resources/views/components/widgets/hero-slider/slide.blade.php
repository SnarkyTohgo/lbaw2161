<div class="item" style="background-image: url({{ $image }});">
    <div class="overlay-black">
        <div class="container">
            <div class="slider-item-content">
                <span class="medium-text">{{ $subheading }}</span>
                <h1 class="big-text">{{ $heading }}</h1>
                <small class="small-text">{{ $fineText }}</small>

                <div class="link-groups">
                    <a href="{{ $slug }}" class="about-btn custom-btn">{{ $cta }}</a>
                    <a href="{{ $secondSlug }}" class="start-btn">{{ $secondCta }}</a>
                </div>
            </div>
        </div>
    </div>
</div>